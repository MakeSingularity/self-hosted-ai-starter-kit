# 🤖 AI Telegram Chatbot Configuration
# Copy this file to .env and update with your values

# ================================
# TELEGRAM CONFIGURATION (Required)
# ================================
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# ================================  
# DATABASE CONFIGURATION (Required)
# ================================
POSTGRES_DB=n8n
POSTGRES_USER=root
POSTGRES_PASSWORD=change_this_to_secure_password

# ================================
# N8N SECURITY (Required)
# ================================
# Generate random 32+ character strings for security
# You can use: openssl rand -base64 32
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here
N8N_USER_MANAGEMENT_JWT_SECRET=your_jwt_secret_key_here

# ================================
# AI MODEL CONFIGURATION
# ================================
# Ollama service endpoint (usually don't change)
OLLAMA_HOST=ollama:11434

# ================================
# NGROK CONFIGURATION (Optional)
# ================================
# For custom domains, get token from https://ngrok.com
NGROK_AUTHTOKEN=your_ngrok_auth_token_here
SUBDOMAIN=your-custom-subdomain-name

# ================================
# ADVANCED SETTINGS (Optional)
# ================================
# Uncomment and modify if needed

# n8n Editor URL (auto-configured via ngrok)
# N8N_EDITOR_BASE_URL=https://your-domain.ngrok-free.app

# Webhook URL (auto-configured via ngrok)  
# N8N_WEBHOOK_URL=https://your-domain.ngrok-free.app

# n8n Security Settings
# N8N_DIAGNOSTICS_ENABLED=false
# N8N_PERSONALIZATION_ENABLED=false

# Docker Compose Profiles (select one):
# - cpu: CPU-only processing
# - gpu-nvidia: NVIDIA GPU acceleration  
# - gpu-amd: AMD GPU acceleration
# COMPOSE_PROFILES=gpu-nvidia

# ================================
# QUICK SETUP GUIDE
# ================================
# 1. Copy this file: cp .env.example .env
# 2. Get Telegram bot token from @BotFather
# 3. Generate secure keys: openssl rand -base64 32
# 4. Update passwords with secure values
# 5. Start with: docker compose --profile gpu-nvidia up -d


